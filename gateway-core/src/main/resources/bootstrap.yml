spring:
  application:
    name: ApiGateway
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: loadbalancer.yaml